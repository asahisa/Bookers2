<div>
  <p>ショップ名：<%= @books.title %></p>
  <p>説明：<%= @books.body %></p>
  <p>投稿ユーザー画像：<%= image_tag 'no_image.jpg' %>
  <p>ユーザーネーム：<%= @books.user.name %></p>
  <p>投稿日：<%= @books.created_at.strftime('%Y/%m/%d') %></p>
</div>